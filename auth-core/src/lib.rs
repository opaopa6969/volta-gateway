//! volta-auth-core — Auth library crate.
//!
//! Phase 0 (DD-005): JWT session verification.
//! Phase 1: Session store, policy engine.
//! Phase 2: OIDC, MFA, Passkey flows (tramli SM).

pub mod jwt;
pub mod session;
